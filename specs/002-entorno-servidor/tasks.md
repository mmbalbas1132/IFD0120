# Desglose de Tareas — 002 · Entorno Servidor (MF0492_3)

**Basado en:** `plan.md` (misma carpeta) y `specs/000-funcional/spec.md` (RF/modelo/contrato §12)
**Rama:** `feature/002-entorno-servidor` — se abre desde `develop` tras mergear
`feature/001-entorno-cliente`.
**Convenciones:** S (≤4h) · M (0.5–2d) · L (>2d) · `[P]` paralelizable · Done verificable.
**Revisado:** 2026-09-23 contra `spec.md` v1.8, tras `/speckit-analyze` (CI, cabeceras y CORS,
edición de uno mismo, orden de adjuntos y errores, contrato de cuerpos). Los IDs no se renumeran:
el orden de ejecución lo dan las dependencias, no el número.

| ID | Descripción | Done | Est. | Deps | Archivos | RF |
|---|---|---|---|---|---|---|
| TS.1 | Inicializar Spring Boot (Web, Data JPA, Security, Validation, springdoc) | `mvn spring-boot:run` levanta `/actuator/health` en 200 | S | — | `backend/pom.xml`, `GestorFpApplication.java` | — |
| TS.2 [P] | `docker-compose.yml` (backend + PostgreSQL) para `dev`, con volúmenes con nombre para los datos de PostgreSQL y para los adjuntos (`gestorfp.adjuntos.directorio`, `plan.md` §5.1) | `docker compose up` levanta ambos servicios; tras `docker compose down` (sin `-v`) y un nuevo `up`, los datos y los ficheros subidos siguen ahí (RNF-008) | S | TS.1 | `docker-compose.yml`, `backend/Dockerfile` | — |
| TS.3 | Migración `V1__init_schema.sql` con todas las tablas de `plan.md` §5 salvo `tokens_revocados` (va en `V3`, TS.27), incluidas las columnas de auditoría de RF-015 | `flyway:migrate` aplica sin error; tablas, índices, `CHECK`, trigger de `historial_evaluaciones` y columnas `fecha_creacion`/`fecha_modificacion` verificados | M | TS.1, TS.2 | `db/migration/V1__init_schema.sql` | RF-008, RF-009, RF-010 |
| TS.4 [P] | Entidades JPA: `Usuario`, `Modulo`, `UnidadFormativa`, `Matricula` | Mapean 1:1 con el esquema; contexto Spring carga sin error | M | TS.3 | `domain/*.java` | RF-008, RF-009, RF-010 |
| TS.5 [P] | Entidades JPA: `Tarea`, `Entrega`, `Evaluacion`, `Recurso`, `Anuncio` | Ídem TS.4 | M | TS.3 | `domain/*.java` | RF-001, RF-003, RF-005, RF-012, RF-013 |
| TS.6 | Repositorios Spring Data JPA para las entidades de TS.4 y TS.5 (las de adjuntos, historial, ciclo y sesiones las crean TS.26, TS.34, TS.37 y TS.8) | Cada repositorio con ≥1 método derivado probado (`@DataJpaTest`) | M | TS.4, TS.5 | `repository/*.java` | — |
| TS.7 | `JwtService` + `SecurityConfig` (filtro JWT, reglas por ruta según §12; access token en cabecera/cuerpo, refresh token en cookie `HttpOnly`) | Login válido → JWT decodificable; ruta protegida sin token → 401 | L | TS.4 | `security/*.java`, `config/SecurityConfig.java` | RF-014 |
| TS.8 | `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout` (exactos a §12 v1.8, logout → `204`; entidad y repositorio `SesionRefresco`; cookies `refresh_token` `HttpOnly`+`Secure`+`SameSite=Strict` y `csrf_token` no-`HttpOnly`, ver `plan.md` §6) | Casos de éxito y error (401) cubiertos con MockMvc; cookies presentes y con atributos correctos en la respuesta, incluido `Path=/api/v1/auth` en `refresh_token` (con `Path=/auth` el navegador no la enviaría); login y refresh devuelven `debeCambiarPassword`; el refresh se valida contra `sesiones_refresco` y el logout revoca la sesión y responde `204` con ambas cookies borradas | M | TS.7 | `controller/AuthController.java`, `domain/SesionRefresco.java`, `repository/SesionRefrescoRepository.java` | RF-014 |
| TS.9 [P] | CRUD `Usuario` con baja lógica (endpoints de §12); el alta genera contraseña temporal (RF-008 v1.5, con `GeneradorPasswordTemporal` de TS.35); la baja de un docente responsable de algún módulo se rechaza; `GET /usuarios` paginado con `pagina`/`tamano` (§12 v1.8); el propio usuario (no ADMINISTRADOR) solo edita `nombre` y `apellidos` | RF-008 probado; email duplicado → 409; `GET /usuarios?pagina=1&tamano=2` → `{ contenido, totalElementos, pagina, tamano }`; un ALUMNO que envía `rol: "ADMINISTRADOR"`, otro `email` o `activo: false` en su propio `PUT` → `200` con solo nombre y apellidos cambiados y el resto intacto en BD; el alta devuelve `passwordTemporal` una sola vez y deja `debeCambiarPassword = true`; baja de docente responsable → `409` sin cambios en BD (la revocación inmediata es TS.40) | M | TS.6, TS.8, TS.22, TS.35 | `{service,controller,dto}/Usuario*.java` | RF-008 |
| TS.10 [P] | CRUD `Modulo` (con `descripcion`, v1.7) + `UnidadFormativa`; `GET /modulos` según §12 v1.8: módulos visibles para cada rol (vía TS.38), `unidadesFormativas` incrustadas y `matriculas` `ACTIVA` solo para el ADMINISTRADOR, filtro `?docenteId=` | RF-009 probado; código duplicado → 409; la descripción se guarda y se devuelve; ADMINISTRADOR recibe `matriculas` con `{ id, alumnoId, alumnoNombre, fechaMatricula }` y DOCENTE y ALUMNO no reciben ese campo; ALUMNO solo ve sus módulos con matrícula `ACTIVA` y DOCENTE solo los que imparte | M | TS.6, TS.8, TS.22, TS.38 | `{service,controller,dto}/Modulo*.java`, `UnidadFormativa*.java` | RF-009 |
| TS.11 | CRUD `Matricula` con índice único parcial (alumno + módulo **con estado `ACTIVA`**, `plan.md` §5); revocar pasa la matrícula a `BAJA` | RF-010; doble matrícula activa → 409; matricular a un DOCENTE o a un ALUMNO dado de baja → `400` sin fila nueva; tras revocarla, volver a matricular al mismo alumno en el mismo módulo → `201` con una fila nueva, y la antigua sigue en `BAJA` | M | TS.9, TS.10 | `{service,controller,dto}/Matricula*.java` | RF-010 |
| TS.12 | `GET /modulos/publicos` (sin auth, sin datos personales) | RF-011: 200 sin token; devuelve código, nombre, descripción y horas; sin campos de alumnado en la respuesta | S | TS.10 | `controller/ModuloPublicoController.java` | RF-011 |
| TS.13 | Seed de datos (`V2__seed_roles_demo.sql`) en la carpeta `db/seed-dev/`, que solo añade `application-dev.yml` a `spring.flyway.locations` (`plan.md` §5) | `docker compose up` en dev deja usuarios/módulos de ejemplo; arrancando con el perfil `prod` contra una BD vacía, Flyway aplica solo `V1` y `V3` y no hay usuarios de ejemplo; la contraseña de desarrollo está documentada en `backend/README.md` | S | TS.3, TS.27 | `db/seed-dev/V2__seed_roles_demo.sql`, `application-dev.yml` | — |
| TS.14 | Validador `@FechaFutura` con `payload = ReglaDeNegocio.class` y solo en el grupo de validación `AlCrear` (`plan.md` §6.1) | Aplicado a `fechaLimite` al crear, no al editar; prueba unitaria aislada con el `Clock` fijado | S | TS.5 | `validation/FechaFuturaValidator.java` | RF-002 |
| TS.15 | Servicio + controlador `Tarea`: crear (se publica al crearla, sin `estado`; `fechaPublicacion` la fija el servidor; `fichero` opcional vía TS.26), editar (`PUT` en JSON con `titulo`, `descripcion` y `fechaLimite`; el fichero no cambia, §12 v1.8) y borrar (`DELETE`) | RF-001/002; fecha límite pasada al crear → `422` con el mensaje «La fecha límite debe ser posterior a la fecha actual» (`@FechaFutura` solo al crear, `plan.md` §6.1); al editar, **ampliar** la fecha límite recalcula `fueraDePlazo` de todas sus entregas, incluidas las calificadas, y **acortarla** con entregas → `409`; borrar sin entregas → `204` (y borra su fichero), con entregas → `409` | L | TS.10, TS.14, TS.22, TS.26, TS.36, TS.38 | `{service,controller,dto}/Tarea*.java` | RF-001, RF-002, RF-004 |
| TS.16 | Servicio + controlador `Entrega`: registrar o reemplazar la entrega (multipart vía TS.26) con cálculo de `fueraDePlazo` | RF-003/004 v1.6: la entrega guarda `estado = ENTREGADA` y `fueraDePlazo` según la hora del servidor (`Clock`), con la igualdad exacta **en plazo**; al calificar, `fueraDePlazo` no cambia; **reemplazo** de una entrega no calificada → `200` con nueva fecha, contenido y plazo, y borra el fichero anterior; entrega ya calificada → `409`; sin fichero ni comentario → `422` con el mensaje «La entrega debe incluir un fichero y/o un comentario» (RF-003 v1.8); alumno con matrícula en `BAJA` → `403`; `GET /tareas/{id}/entregas` devuelve `{ entregas, sinEntregar }`, donde `sinEntregar` es el alumnado con matrícula `ACTIVA` sin entrega (RF-005 v1.7): un alumno sin entrega aparece ahí y deja de aparecer al entregar, y no aparecen ni uno con matrícula en `BAJA` ni un usuario dado de baja (`activo = false`, RF-005 v1.8) | M | TS.15, TS.22, TS.26, TS.36, TS.38 | `{service,controller,dto}/Entrega*.java` | RF-003, RF-004 |
| TS.17 | Validador `@RangoCalificacion` (0–10, un decimal) con `payload = ReglaDeNegocio.class` (`plan.md` §6.1) | Prueba unitaria con límites 0, 10, -0.1, 10.1 y 8.55 (más de un decimal) | S | TS.5 | `validation/RangoCalificacionValidator.java` | RF-006 |
| TS.18 | Servicio + controlador `Evaluacion` con comprobación de propiedad (docente del módulo) | RF-005/006; docente ajeno → 403; nota fuera de rango → `422` con el mensaje «La calificación debe estar entre 0 y 10» (la misma respuesta que el mock de `001`); la entrega sigue en `ENTREGADA` | M | TS.16, TS.17, TS.22 | `{service,controller,dto}/Evaluacion*.java` | RF-005, RF-006 |
| TS.19 | `GET /alumnos/{id}/calificaciones` con filtro de propiedad | RF-007: alumno solo ve las suyas (403 si otro id), **incluidas las de módulos con la matrícula en `BAJA`**; docente ve las de su módulo | M | TS.18, TS.38 | `controller/CalificacionController.java` | RF-007 |
| TS.20 [P] | Servicio + controlador `Recurso`: publicar (DOCUMENTO con `fichero` vía TS.26; VIDEO/ENLACE con `url`), editar (`PUT /recursos/{id}`) y borrar (`DELETE /recursos/{id}`) | RF-012; tipos DOCUMENTO/VIDEO/ENLACE probados; editar cambia título, descripción y URL pero **no** el tipo; borrar un DOCUMENTO borra también su fichero; docente de otro módulo → `403` | M | TS.10, TS.22, TS.26, TS.38 | `{service,controller,dto}/Recurso*.java` | RF-012 |
| TS.21 [P] | Servicio + controlador `Anuncio` (orden por destacado): publicar, editar (`PUT /anuncios/{id}`) y borrar (`DELETE /anuncios/{id}`) | RF-013; orden de listado probado; editar título, contenido y destacado; borrar → `204`; docente de otro módulo → `403` | M | TS.10, TS.22, TS.38 | `{service,controller,dto}/Anuncio*.java` | RF-013 |
| TS.22 | **Base, antes de cualquier controlador de negocio:** `GlobalExceptionHandler` + `AuthenticationEntryPoint` y `AccessDeniedHandler` propios, excepciones de dominio y *payload* `ReglaDeNegocio`, unificando el contrato de error de §12 (tabla completa en `plan.md` §6.1) | Toda excepción mapeada a 400/401/403/404/409/413/415/422/429/500 con el cuerpo de §12; una prueba por código con un controlador de prueba (no depende de los de negocio); infracciones con `payload = ReglaDeNegocio` → `422` y el resto de validaciones → `400`; los `401`/`403` de los filtros de seguridad llevan el mismo cuerpo; `429` incluye `Retry-After`; `500` no filtra detalles internos | M | TS.7 | `exception/*.java`, `security/{EntryPointJson,AccessDeniedHandlerJson}.java`, `validation/ReglaDeNegocio.java` | Contrato §12 |
| TS.23 | Auditoría mínima (`fechaCreacion`/`fechaModificacion`) vía `@EntityListeners` | RF-015 verificado en **todas** las entidades principales (`Usuario`, `Ciclo`, `Modulo`, `UnidadFormativa`, `Matricula`, `Tarea`, `Entrega`, `Evaluacion`, `Recurso`, `Anuncio`): una prueba parametrizada comprueba que crear rellena ambas fechas y modificar solo cambia `fechaModificacion` | S | TS.4, TS.5, TS.37 | `domain/Auditable.java` | RF-015 |
| TS.24 | Cobertura JaCoCo `service` ≥ 70% como *gate* de CI | Reporte JaCoCo en CI con umbral bloqueante: bajar de 70% hace fallar `mvn verify` y el job de TS.41 | M | TS.14–TS.23, TS.41 | `pom.xml` (plugin JaCoCo) | RNF-007 |
| TS.25 | Verificación de conformidad (`plan.md` §7): OpenAPI generado vs. `spec.md` §12 (métodos, rutas, roles, códigos) **y** forma de los cuerpos vs. los handlers de MSW de `001` (§12 v1.8, «Forma de los cuerpos») | `docs/openapi-generado.json` exportado en el build; tabla de comparación en `docs/conformidad-contrato.md` con los 37 endpoints: mismos métodos, rutas, roles y códigos, y mismos nombres de campo en petición y respuesta que el mock; 0 discrepancias sin justificar | M | TS.8–TS.23, TS.26, TS.34, TS.35, TS.37, TS.43 | `docs/openapi-generado.json`, `docs/conformidad-contrato.md` | Contrato §12 |
| TS.26 | **Base de adjuntos, antes de Tarea, Entrega y Recurso** (RNF-014, `plan.md` §5.1): entidad y repositorio `Adjunto`; `AdjuntoService` guarda en el volumen configurado (`gestorfp.adjuntos.directorio`) con el UUID como nombre; detección del tipo real con `tika-core` y lista blanca; límite de 10 MB; borrado físico registrado para después del commit. La descarga es TS.43 | Pruebas de servicio: fichero de 10 MB exactos se acepta y de 10 MB + 1 byte → `413`; un `.pdf` que en realidad es un ejecutable → `415` aunque declare `application/pdf`; un nombre con `../` no sale del directorio; si la transacción hace *rollback*, el fichero no se borra, y tras el commit sí | M | TS.6, TS.22 | `service/AdjuntoService.java`, `service/DetectorTipoFichero.java`, `domain/Adjunto.java`, `repository/AdjuntoRepository.java` | RNF-014 |
| TS.27 | Migración `V3__tokens_revocados.sql` + entidad/repositorio `TokenRevocado` (`jti`, `fecha_expiracion`) | `flyway:migrate` aplica sin error; repositorio probado con `@DataJpaTest` | S | TS.3 | `db/migration/V3__tokens_revocados.sql`, `domain/TokenRevocado.java`, `repository/TokenRevocadoRepository.java` | RNF-002 |
| TS.28 | Filtro de replay: tras validar firma/expiración del JWT, rechaza (`401`) si su `jti` está en `tokens_revocados`; `POST /auth/logout` inserta el `jti` vigente | Prueba: token revocado tras logout → `401` en petición posterior con el mismo access token | M | TS.7, TS.27 | `security/JtiBlacklistFilter.java`, `controller/AuthController.java` | RNF-002 |
| TS.29 | Job periódico (`@Scheduled`) de limpieza de `tokens_revocados` con `fecha_expiracion` pasada | Prueba de servicio: filas expiradas se eliminan, filas vigentes no | S | TS.27 | `service/LimpiezaTokensRevocadosJob.java` | RNF-002 |
| TS.30 | Rate limiter Bucket4j **solo** en `POST /auth/login` (RNF-011 v1.8: clave IP+usuario, 5 intentos fallidos admitidos en 15 min, el 6.º → `429`) | Prueba: tras 5 intentos fallidos, el 6.º en la ventana → `429` con `Retry-After`, aunque la contraseña sea correcta; mismo usuario desde otra IP → no bloqueado; intento tras la ventana → `200`/`401` normal; `/auth/refresh` no está limitado | M | TS.8 | `security/LoginRateLimiterFilter.java` | RNF-011 |
| TS.31 | Filtro CSRF de doble token: exige `X-CSRF-Token` = cookie `csrf_token` en **toda** petición `POST`/`PUT`/`DELETE` bajo `/api/v1`, salvo `POST /api/v1/auth/login`; sin exención por llevar `Authorization: Bearer` (Principio 5, `plan.md` §6) | Prueba: mutación **con `Bearer` válido** pero sin cabecera o con valor distinto → `403`; con valor correcto → pasa; `POST /auth/login` sin cookie → no exige CSRF | M | TS.8 | `security/CsrfDobleTokenFilter.java` | RNF-003 |
| TS.32 | Prueba de propiedad de recurso (`403`) por cada endpoint sensible: Entrega, Evaluación, Calificación, historial de calificación, descarga de adjuntos, creación/edición/borrado de tareas, recursos y anuncios, contraseña de otro usuario, detalle y edición de otro usuario (`GET`/`PUT /usuarios/{id}`), matrículas en `GET /modulos` y acceso con matrícula en `BAJA` (RNF-012) | Matriz de pruebas: usuario autenticado con rol correcto pero recurso ajeno → `403` en cada endpoint listado; un ALUMNO que edita a otro usuario → `403`, y en su propio `PUT` no cambia `rol`, `email` ni `activo`; DOCENTE y ALUMNO no reciben `matriculas` en `GET /modulos`; con matrícula en `BAJA`, `403` en contenido del módulo y `200` en sus calificaciones y ficheros propios | M | TS.9, TS.10, TS.16, TS.18, TS.19, TS.20, TS.21, TS.34, TS.35, TS.38, TS.43 | `controller/*Test.java` | RNF-012 |
| TS.33 | Mecanismo de anonimización en baja lógica de `Usuario` (sustituye nombre/apellidos/email, conserva id/rol) invocable manualmente desde `service` | Prueba: tras invocar, `nombre`/`apellidos`/`email` no identificables; `id`/`rol` intactos; entregas/evaluaciones ya emitidas siguen íntegras (FK) | M | TS.9 | `service/AnonimizacionUsuarioService.java` | RNF-013 |
| TS.34 | Historial de calificaciones (RF-016): tabla `historial_evaluaciones` de solo inserción (trigger que rechaza `UPDATE`/`DELETE`), inserción en la misma transacción que registra o edita la evaluación, y `GET /entregas/{id}/evaluacion/historial` | Pruebas: calificar y luego editar deja 2 filas (anterior nula en la primera) con autor y fecha; el orden es del más reciente al más antiguo; un `UPDATE` directo en la tabla falla; alumno → `403`; docente de otro módulo → `403`; ADMINISTRADOR → `200` | M | TS.18 | `domain/HistorialEvaluacion.java`, `repository/HistorialEvaluacionRepository.java`, `service/EvaluacionService.java`, `controller/EvaluacionController.java` | RF-016 |
| TS.35 | Contraseñas (RF-017, RF-018): `GeneradorPasswordTemporal` (`SecureRandom`, 12 caracteres que cumplen RF-018); `POST /usuarios/{id}/restablecer-password` (devuelve la temporal una vez); `PUT /usuarios/{id}/password` con `@PasswordSegura`, comprobación de la actual y de que sea distinta; filtro que responde `403 CAMBIO_PASSWORD_REQUERIDO` salvo `/auth/**` y el cambio propio | Pruebas: 1.000 temporales generadas cumplen RF-018; tras restablecer, la contraseña anterior deja de servir; con `debeCambiarPassword` cualquier otra ruta → `403 CAMBIO_PASSWORD_REQUERIDO` y tras cambiarla → acceso normal; cada regla de RF-018 incumplida → `400` con su mensaje; contraseña actual incorrecta → `400`; solo el ADMINISTRADOR restablece (resto → `403`); cambiar la contraseña de otro usuario → `403` | M | TS.6, TS.8 | `service/{GeneradorPasswordTemporal,PasswordService}.java`, `validation/PasswordSeguraValidator.java`, `security/CambioPasswordObligatorioFilter.java`, `controller/UsuarioController.java` | RF-008, RF-017, RF-018 |
| TS.36 | Fechas en UTC (RNF-010, `plan.md` §5.2): `Instant` + `TIMESTAMPTZ`, `hibernate.jdbc.time_zone=UTC`, Jackson ISO 8601 con `Z` y rechazo de fechas sin zona, `Clock` inyectable para la hora del servidor | Pruebas: toda fecha de la API sale con `Z`; una `fechaLimite` sin zona → `400`; con el `Clock` fijado, entrega a la hora límite exacta → en plazo y un segundo después → fuera de plazo; mismos resultados en fechas de horario de invierno y de verano y con la JVM arrancada en otra zona (`-Duser.timezone=America/New_York`) | S | TS.1, TS.3 | `config/{JacksonConfig,ClockConfig}.java`, `application.yml` | RF-004, RNF-010 |
| TS.37 | Información del ciclo (RF-019, RF-011 v1.7): tabla `ciclo` con un único registro (lo inserta la migración), `GET /ciclo` público y `PUT /ciclo` solo ADMINISTRADOR, texto plano | Pruebas: `GET /ciclo` sin token → `200` con horario y requisitos; `PUT /ciclo` como ADMINISTRADOR cambia lo que devuelve `GET`; como DOCENTE o ALUMNO → `403`; sin token → `401`; no existe forma de crear un segundo registro | S | TS.3, TS.8 | `domain/Ciclo.java`, `repository/CicloRepository.java`, `{service,controller,dto}/Ciclo*.java` | RF-011, RF-019 |
| TS.38 | `AccesoModuloService` (RF-007, RF-010 v1.6, `plan.md` §6): reglas únicas de acceso a un módulo para todos los servicios; «matriculado» = matrícula `ACTIVA`; con `BAJA`, el alumno conserva solo sus entregas, calificaciones y ficheros propios | Pruebas de servicio (la comprobación por endpoint es TS.32): alumno con matrícula `ACTIVA` puede ver el contenido del módulo; con la matrícula en `BAJA` no puede ver contenido ni entregar, pero sí sus entregas, calificaciones y ficheros propios; docente no responsable → sin acceso; ADMINISTRADOR → acceso total; ningún servicio consulta matrículas por su cuenta (revisión en el PR) | M | TS.6, TS.22 | `service/AccesoModuloService.java` | RF-007, RF-010, RNF-012 |
| TS.39 | Javadoc obligatorio (Principio 7): Maven Checkstyle Plugin con la regla `MissingJavadocType` sobre `service` y `controller`, ejecutado en `mvn verify` y en CI | Una clase pública de `service` sin Javadoc hace fallar `mvn verify` (comprobado a propósito); el build de CI la bloquea | S | TS.1, TS.41 | `pom.xml`, `checkstyle.xml` | Principio 7 |
| TS.40 | Revocación inmediata en la baja de usuario (RF-008 v1.6, `plan.md` §6): el filtro de autenticación rechaza con `401` a un usuario no `activo` en cada petición; la baja marca como revocadas todas sus `sesiones_refresco` | Pruebas: con un access token todavía vigente, tras la baja la siguiente petición → `401`; `/auth/refresh` con su cookie → `401`; sus entregas, calificaciones y matrículas siguen intactas | S | TS.8, TS.9, TS.28 | `security/JtiBlacklistFilter.java`, `service/UsuarioService.java` | RF-008, RNF-002 |
| TS.41 | Integración continua (Principios 6 y 7): workflow `.github/workflows/ci.yml` que en cada PR contra `develop` y `main` ejecuta `mvn verify` del backend (pruebas con Testcontainers, JaCoCo y Checkstyle) y las pruebas del frontend ya existentes (`npm run lint`, `npm run test`, `npm run test:a11y`); `003` lo amplía en TI.16 | Un PR de prueba con una prueba rota, con la cobertura de `service` bajo el 70% o con una clase de `service` sin Javadoc sale en rojo; en verde, el job pasa; la rama `develop` exige el check (configurado en GitHub y documentado en `backend/README.md`) | S | TS.1 | `.github/workflows/ci.yml`, `backend/README.md` | RNF-007, Principios 6 y 7 |
| TS.42 | CORS y cabeceras de seguridad (Principio 5, `plan.md` §6): CORS con el origen del `frontend` por perfil (`gestorfp.cors.origenes`) y `Access-Control-Allow-Credentials: true`; cabeceras CSP, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY` y `Strict-Transport-Security` en todas las respuestas, en todos los perfiles. `003` las endurece (TI.6), no las introduce | Pruebas MockMvc: una respuesta cualquiera lleva las cuatro cabeceras; *preflight* desde el origen configurado → permitido con `Allow-Credentials: true`; desde otro origen → rechazado (sin `Access-Control-Allow-Origin`) | S | TS.7 | `config/{SecurityConfig,CorsConfig}.java`, `application-*.yml` | RNF-003, Principio 5 |
| TS.43 | Descarga de adjuntos y cierre de su integración (RNF-014, `plan.md` §5.1): `GET /adjuntos/{id}` con los permisos del elemento al que pertenece (vía TS.38); comprobación de extremo a extremo de la subida desde Tarea, Entrega y Recurso DOCUMENTO | Pruebas: descargar con permiso → `200` con `Content-Disposition: attachment`, el tipo detectado y `nosniff`; sin permiso → `403`; alumno con matrícula en `BAJA` descarga su propio fichero → `200`; tras reemplazar una entrega, o borrar una tarea o un recurso DOCUMENTO, el fichero ya no existe en el volumen; ningún adjunto se sirve como estático | M | TS.15, TS.16, TS.20, TS.26, TS.38 | `controller/AdjuntoController.java` | RF-001, RF-003, RF-012, RNF-014 |

## Cierre del módulo

Antes de abrir el PR de `feature/002-entorno-servidor` → `develop`:

- [ ] Los 37 endpoints de §12 (spec v1.8) implementados y probados (éxito + error), y la tabla de
  conformidad de TS.25 sin discrepancias sin justificar.
- [ ] CI (TS.41) en verde y exigido en `develop`, con cobertura `service` ≥ 70% (TS.24) y Javadoc
  en `service` y `controller` verificado por Checkstyle (TS.39).
- [ ] `/swagger-ui.html` navegable en `dev`.
- [ ] TS.26 y TS.43 (adjuntos) implementadas con su volumen persistente y probadas.
- [ ] TS.27–TS.33, TS.35, TS.40 y TS.42 (seguridad: replay, rate limiting, CSRF en toda mutación,
  propiedad de recurso, anonimización, cambio de contraseña obligatorio, baja inmediata, CORS y
  cabeceras) implementadas y probadas — Constitución Principio 5 v2.1.0 / ADR-0002.
